class AnsweredDetail {
  AnsweredDetail(this.id,this.answeredAnswer, this.isCorrect,this.question, this.correctAnswer);
  final int id;
  final String answeredAnswer;
  final bool isCorrect;
  final String question;
  final String correctAnswer;
}